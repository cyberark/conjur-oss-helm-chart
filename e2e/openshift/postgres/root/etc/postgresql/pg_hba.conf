# PostgreSQL Client Authentication Configuration File
# ===================================================
hostssl replication     all             all                     cert clientcert=1
hostssl conjur          all             all                     cert clientcert=1

# "local" is for Unix domain socket connections only
local   all             all                                     peer
